<%= form_for(@spread) do |f| %>
  <% if @spread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spread.errors.count, "error") %> prohibited this spread from being saved:</h2>

      <ul>
      <% @spread.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :master_spread %><br>
    <%= f.check_box :master_spread %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
